import { database } from "@/lib/database/client";import { recordContentInteraction } from "./content-interactions";
export async function recordFeedInteraction(input:{projectId:string;externalUserId:string;itemType:"first_party"|"external_news";itemId:string;interaction:string}){if(input.itemType==="first_party")return recordContentInteraction({projectId:input.projectId,externalUserId:input.externalUserId,contentId:input.itemId,interaction:input.interaction});return database.newsInteraction.create({data:{projectId:input.projectId,externalUserId:input.externalUserId,externalArticleId:input.itemId,interactionType:input.interaction.toUpperCase() as never}})}
